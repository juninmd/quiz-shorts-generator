name: 'Setup Ollama with Model'
description: 'Instala Ollama, aguarda inicialização, pulls modelo e configura cache'

inputs:
  model:
    description: 'Nome do modelo Ollama a usar (ex: gemma3:1b, llama2, etc)'
    required: true
  ollama-host:
    description: 'Host do Ollama (padrão: http://localhost:11434)'
    required: false
    default: 'http://localhost:11434'
  wait-timeout:
    description: 'Timeout máximo para Ollama ficar pronto, em segundos'
    required: false
    default: '120'
  cache-enabled:
    description: 'Habilitar cache de modelos (true/false)'
    required: false
    default: 'true'

outputs:
  ollama-ready:
    description: 'Booleano indicando se Ollama está pronto'
    value: ${{ steps.ollama-ready.outputs.ready }}
  model-cached:
    description: 'Booleano indicando se modelo veio do cache'
    value: ${{ steps.cache-restore.outputs.cache-hit }}

runs:
  using: 'composite'

  steps:
    - name: Restore Ollama Models Cache
      if: inputs.cache-enabled == 'true'
      id: cache-restore
      uses: actions/cache/restore@v4
      with:
        path: /home/runner/.ollama/models
        key: ollama-models-${{ inputs.model }}

    - name: Install Ollama
      shell: bash
      run: |
        echo "📥 Instalando Ollama..."
        curl -fsSL https://ollama.com/install.sh | sh
        echo "✅ Ollama instalado"

    - name: Start Ollama Service
      shell: bash
      run: |
        echo "🚀 Iniciando serviço Ollama..."
        ollama serve > /tmp/ollama.log 2>&1 &
        echo $! > /tmp/ollama.pid
        echo "Ollama PID: $(cat /tmp/ollama.pid)"

    - name: Wait for Ollama Ready
      id: ollama-ready
      shell: bash
      run: |
        TIMEOUT=${{ inputs.wait-timeout }}
        OLLAMA_HOST="${{ inputs.ollama-host }}"

        echo "⏳ Aguardando Ollama estar pronto em $OLLAMA_HOST (máximo ${TIMEOUT}s)..."

        for i in $(seq 1 $TIMEOUT); do
          if curl -s "$OLLAMA_HOST/api/tags" > /dev/null 2>&1; then
            echo "✅ Ollama está pronto! (${i}s)"
            echo "ready=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [ $((i % 10)) -eq 0 ]; then
            echo "  ⏳ Ainda aguardando... $((TIMEOUT - i))s restantes"
          fi

          sleep 1
        done

        echo "❌ Timeout: Ollama não respondeu em ${TIMEOUT}s"
        echo "📋 Log do Ollama:"
        cat /tmp/ollama.log || echo "  (Log não disponível)"
        echo "ready=false" >> $GITHUB_OUTPUT
        exit 1

    - name: Pull Ollama Model
      shell: bash
      run: |
        if [ "${{ steps.cache-restore.outputs.cache-hit }}" == 'true' ]; then
          echo "📦 Modelo ${{ inputs.model }} carregado do cache"
        else
          echo "📥 Puxando modelo ${{ inputs.model }}..."
          ollama pull ${{ inputs.model }}
          echo "✅ Modelo puxado com sucesso"
        fi

    - name: List Ollama Models
      shell: bash
      run: |
        echo "📋 Modelos disponíveis:"
        ollama list

    - name: Setup Cleanup Hook
      shell: bash
      run: |
        # Salva o PID para uso em cleanup posterior
        echo "${{ inputs.model }}" > /tmp/ollama-model.txt
